# Core LangChain ecosystem
langchain==0.1.17
langgraph==0.3.20
langsmith==0.3.19
langchain-openai==0.1.6  # Compatible with openai 1.68.2
openai==1.68.2

# Hugging Face + model integration
huggingface-hub==0.29.3

# System and typing
cffi==1.17.1
typing==3.10.0.0  # Only needed for older Python; can omit if using 3.10+
typing_extensions==4.12.2

# Scientific + progress
numpy==1.26.4  # numpy 2.2.4 doesn't exist yet; this is latest stable
tqdm==4.67.1

# YAML support
PyYAML==6.0.2  # Use this instead of lowercase `yaml==0.2.5`

# CLI + HTTP
typer==0.15.2
requests==2.32.3

# Git + serialisation
gitpython==3.1.44
pydantic==2.10.6
annotated-types==0.7.0

# Experiment tracking + system stats
wandb==0.19.8
psutil==5.9.8  # psutil 7.0.0 does not exist yet
